Haz una programa que declara un array de longitud 10 y que rellena todo las posiciones
con un valor entre 1 a 100. Despues enseñalo

int lista [] = new int [10], total = 0;
        int length = lista.length;
        
        for(int i = 0; i<length;i++ ){
            int numR = (int) (Math.random() * 100 + 1);
            lista [i]= numR;
        }
        for(int i = 0; i<length;i++ ){
            total += lista[i];
        }
        System.out.println("La suma de de los numeros de la lista serian: "+total);

Fes un programa que demani la introducció de 5 nombres sencers. Després 
s’han de mostrar els 5 valors, un per línia, seguint l’ordre en que s’han introduït.

Scanner sc = new Scanner(System.in);

        int num;
        int lista[] = new int[5];
        int length = lista.length;
        
        System.out.println("Dime 5 numeros enteros: ");
        for (int i = 0; i < length; i++) {
            num = sc.nextInt();
            lista[i] = num;
        }
        System.out.println("los numeros que has introducidos son: ");
        for (int i = 0; i < length; i++) {
            System.out.println(lista[i]);
        }
;

Fes un programa que demani quants nombres s’han d’introduïr. Després ha de 
demanar la introducció de tots els nombres. Finalment ha de mostrar per pantalla 
tots els nombres, un per línia, en l’ordre invers d’introducció.

Scanner sc = new Scanner(System.in);
        
        int num, veces;
        
        System.out.println("Dime cuantos numeros quieres introducir: ");
        veces= sc.nextInt();
        int lista[] = new int[veces];
        
        System.out.println("Ahora dime "+veces+" numeros que quieres introducir: ");
        for (int i = 0; i < veces; i++) {
            num= sc.nextInt();
            lista[i] = num;
        }
        System.out.println("los numeros que has introducidos son: ");
        for (int i = veces -1; i >= 0; i--) {
            System.out.println(lista[i]);
        }
;

Seguint l’exercici anterior, enlloc de mostrar tots els nombres, 
mostra només el valor màxim introduït.

Scanner sc = new Scanner(System.in);
        
        int num, veces, mayor=0, menor=0;
        
        System.out.println("Dime cuantos numeros quieres introducir: ");
        veces= sc.nextInt();
        int lista[] = new int[veces];
        
        System.out.println("Ahora dime "+veces+" numeros que quieres introducir: ");
        for (int i = 0; i < veces; i++) {
            num= sc.nextInt();
            lista[i] = num;
            mayor = (num > menor) ? num : menor;
            menor= num;
        }
        System.out.println("los numeros que has introducidos son: ");
        for (int i = veces -1; i >= 0; i--) {
            System.out.println(lista[i]);
        }
        System.out.println("El numero maximo es: "+mayor);
;